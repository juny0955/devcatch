spring:
  mail:
    host: ${MAIL_HOST:123}
    port: ${MAIL_PORT:123}
    username: ${MAIL_USERNAME:test}
    password: ${MAIL_PASSWORD:test}
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true